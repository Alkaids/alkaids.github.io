
language: node_js
node_js: stable
# S: Build Lifecycle
install:
  - npm install
#before_script:
# - npm install -g gulp
script:
  - npm run build
after_script:
  - cd dist
  - git init
  - git config user.name "gravel"
  - git config user.email "la9249@163.com"
  - git add .
  - git commit -m 'deploy'
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle
branches:
  only:
    - dev
env:
  global:
    - GH_REF: github.com/Alkaids/Alkaids.github.io.git
